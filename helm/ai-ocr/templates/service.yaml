apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-ocr.fullname" . }}
  labels:
    {{- include "ai-ocr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-ocr.selectorLabels" . | nindent 4 }}
